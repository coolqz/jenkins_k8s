apiVersion: v1
kind: Service
metadata:
  labels:
    app: javademo
  name: javademo-service
  namespace: default
spec:
  ports:
  - name: 8080-8080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: javademo
  type: NodePort

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: javademo
  name: javademo
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: javademo
  template:
    metadata:
      labels:
        app: javademo
    spec:
      containers:
      - image: 192.168.100.203/test/javademo:v1
        imagePullPolicy: IfNotPresent
        name: javademo
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256M"
          limits:
            memory: "512M"
      imagePullSecrets:
      - name: harborsecret